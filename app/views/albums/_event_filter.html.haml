= link_to root_path do
    %div{:class => ["rounded-md mr-3 mt-2 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 cursor-pointer", (params[:eventIds] ? 'bg-indigo-400' : 'bg-indigo-600' )]}
        All Events
- @events.each do |event|
    =link_to root_path(eventIds: event.id) do
        %div{:class => ["rounded-md mr-3 mt-2 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600 cursor-pointer", (params[:eventIds] == event.id.to_s ? 'bg-indigo-600' : 'bg-indigo-400')]}
            = event.name